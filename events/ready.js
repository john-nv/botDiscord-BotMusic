module.exports = async (client) => {

    console.log(`Sẵn sàng vào ${client.guilds.cache.size} máy chủ, với tổng số ${client.users.cache.size} người dùng`);

    // client.user.setActivity(client.config.game);

};